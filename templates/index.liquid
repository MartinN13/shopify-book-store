<div class="popular-by-genre">
  {% if collections.size > 0 %}
    {% include 'list-collections' %}
  {% endif %}
</div>

{% assign collection = collections.all %}
<div class="row">
  {% for product in collection.products | limit:8 %}
    {% include 'product-loop' %}
  {% endfor %}
</div>
